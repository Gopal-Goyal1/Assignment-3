# Assignment-3
This repo includes the task 1 which defines a function that calculates the factorial of a number the user inputs and task 2 which imports the math module and perform different functions on it such as sqrt, log and sine
